#include "main.h"

/**
 * swap_int - functiion that swap two integers 
 * @a: first pointer int
 * @b: second pointer int
 * Return: nothing(void)
 */

   void swap_int(int *a, int *b)
   {
	   	int 1;

		i = *a;
		*a = *b;
		*b = i;
   } 
